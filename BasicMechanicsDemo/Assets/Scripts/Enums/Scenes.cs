/*
* An enum to contain all the scene indices in the build settings.
* Note that this implies that the scenes in the build settings should have the same indices as this enum.
*/

public enum Scenes {
	DEMO_AREA = 0,
	MAIN_MENU,
	FOREST,
	OVERWORLD,
	GAME_OVER,
	CASTLE
}
